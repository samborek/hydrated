import { AssetCapData } from "@/hooks/commonTypes";
type BorrowCapMaxedTooltipProps = {
    borrowCap: AssetCapData;
};
export declare const BorrowCapMaxedTooltip: ({ borrowCap, }: BorrowCapMaxedTooltipProps) => import("react").JSX.Element | null;
export {};
