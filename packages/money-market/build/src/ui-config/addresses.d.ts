export declare const AaveV3HydrationTestnet: {
    POOL_ADDRESSES_PROVIDER: string;
    POOL: string;
    WETH_GATEWAY: string;
    FAUCET: string;
    WALLET_BALANCE_PROVIDER: string;
    UI_POOL_DATA_PROVIDER: string;
    UI_INCENTIVE_DATA_PROVIDER: string;
    GHO_TOKEN_ADDRESS: string;
    GHO_UI_DATA_PROVIDER: string;
    COLLECTOR: string;
};
export declare const AaveV3HydrationMainnet: {
    POOL_ADDRESSES_PROVIDER: string;
    POOL: string;
    WETH_GATEWAY: string;
    FAUCET: string;
    WALLET_BALANCE_PROVIDER: string;
    UI_POOL_DATA_PROVIDER: string;
    UI_INCENTIVE_DATA_PROVIDER: string;
    GHO_TOKEN_ADDRESS: string;
    GHO_UI_DATA_PROVIDER: string;
    COLLECTOR: string;
};
