import { jsx as _jsx } from "@galacticcouncil/ui/jsx/jsx-runtime";
import { Slot } from "@radix-ui/react-slot";
export const Box = ({ css, sx, as: Component = "div", asChild = false, m, mt, mr, mb, ml, mx, my, p, pt, pr, pb, pl, px, py, display, position, color, bg, top, right, bottom, left, transform, width, height, minWidth, maxWidth, maxHeight, size, borderRadius, borderWidth, borderStyle, borderColor, alignContent, alignItems, gap, flex, visibility, gridColumn, gridRow, ...rest }) => {
    const SlottedComponent = asChild ? Slot : Component;
    const fullSx = {
        ...sx,
        m,
        mt,
        mr,
        mb,
        ml,
        mx,
        my,
        p,
        pt,
        pr,
        pb,
        pl,
        px,
        py,
        display,
        position,
        color,
        bg,
        top,
        right,
        bottom,
        left,
        transform,
        width,
        height,
        minWidth,
        maxWidth,
        maxHeight,
        size,
        borderRadius,
        borderWidth,
        borderStyle,
        borderColor,
        alignContent,
        alignItems,
        gap,
        flex,
        visibility,
        gridColumn,
        gridRow,
    };
    return _jsx(SlottedComponent, { sx: fullSx, css: css, ...rest });
};
