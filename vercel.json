{
    "buildCommand": "yarn turbo run build --filter=@galacticcouncil/main",
    "outputDirectory": "apps/main/build",
    "framework": "vite",
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}